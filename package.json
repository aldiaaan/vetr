{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.6.4"
  },
  "scripts": {
    "install": "lerna bootstrap && lerna run build",
    "start:server": "npm run build:server && node ./apps/vetr-server/dist/app.js"
  }
}
