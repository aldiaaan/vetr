{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.6.4"
  },
  "scripts": {
    "heroku-postbuild": "lerna bootstrap && lerna run build",
    "start:server": "node ./apps/vetr-server/dist/app.js"
  }
}
